{
  "name": "earnings-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
